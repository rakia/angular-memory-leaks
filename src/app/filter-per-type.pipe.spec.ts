import { FilterPerTypePipe } from './filter-per-type.pipe';

describe('FilterPerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
